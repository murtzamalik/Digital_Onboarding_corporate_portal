# Corporate Portal

React 18 + TypeScript + Vite app for corporate users.

## Setup

```bash
npm install
```

## Development

```bash
npm run dev
```

Copy `.env.example` to `.env` and adjust `VITE_API_BASE_URL` if needed.
